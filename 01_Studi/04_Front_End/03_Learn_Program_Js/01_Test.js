/*let count = 0;
for(let i = 1; i <= 11; i++) {
    const remainder = i % 2;
    const isEven = remainder === 0;
    if(isEven) {
        count++;
    }
}
console.log(count); // 5.*/

function isEven(num) {
    const remainder = num % 2;
    const isEven = remainder === 0;    
    if (isEven) {
        true
        
    }
    console.log(remainder)
}

console.log(isEven(4))