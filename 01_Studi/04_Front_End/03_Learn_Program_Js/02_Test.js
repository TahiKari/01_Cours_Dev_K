/* Les fonctions */

// Que fais ce code ?
/*function ecrireTitre(titre) {
    console.log("-- " + titre + " --");
  }
  function ecrireArticle(nomArticle, prixHT) {
    console.log(nomArticle + " : " + prixHT + " €");
  }
  
  function calculerTVA(prixHT) {
    let TVAEnpourcent = 21;
    return prixHT * TVAEnpourcent / 100
  }
  
  let totalHT = 0;
  let totalTVA = 0;
  let totalTTC = 0;
  let prixChocolatChaud = 2;
  let tarteAuxPommes = 5;
  
  ecrireTitre("Liste d'articles v0");
  
  ecrireArticle("Chocolat chaud", prixChocolatChaud);
  totalHT = totalHT + prixChocolatChaud;
  totalTVA = totalTVA + calculerTVA(prixChocolatChaud);
  
  ecrireArticle("Tarte aux pommes", tarteAuxPommes);
  totalHT = totalHT + tarteAuxPommes ;
  totalTVA = totalTVA + calculerTVA(tarteAuxPommes);
  
  ecrireTitre("Montant total");
  
  totalTTC = totalHT + totalTVA;
  
  console.log("Net à payer : " + totalTTC + " €");
  console.log("Dont TVA : " + totalTVA + " €");*/

  